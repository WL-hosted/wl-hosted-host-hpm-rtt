mainmenu "RT-Thread Configuration"

BSP_DIR := .

RTT_DIR := rt-thread

PKGS_DIR := packages

source "$(RTT_DIR)/Kconfig"
osource "$PKGS_DIR/Kconfig"
rsource "packages/wlh_rtt_host/Kconfig"
rsource "libraries/Kconfig"
rsource "board/Kconfig"
